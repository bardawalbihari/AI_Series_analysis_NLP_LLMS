# Notes

This file is kept only as a short reference for older setup work.

- Current setup and run instructions are in README.md and QUICK_START.md.
- The main application entry point is gradio_app.py.
- The current chatbot path uses grounded local retrieval with optional hosted inference.
- Windows startup is available through run_app.bat.
- Git Bash and Unix-style startup are available through run_app.sh.

For local setup on a new machine, use the instructions in README.md or run the launcher scripts directly.
